apiVersion: v1
kind: Secret
metadata:
  name: fcg-shared-secret
  namespace: fcg
type: Opaque
stringData:
  Jwt__SecretKey: "UvPTu5UZIcSe0V1onJSNTWT579OHlmoxXA1flLgKpow="
  RabbitMq__Password: "guest"
  # Credenciais do IAM user "fcg-services" (só sqs:SendMessage). Nomes exigidos
  # pela cadeia de resolução de credenciais do AWS SDK, não pelo padrão Config__Nested.
  # NÃO commite valores reais aqui — diferente do Jwt__SecretKey acima, uma chave AWS
  # é válida contra a API real da AWS. Edite localmente após aplicar e nunca commite
  # o resultado (o GitHub Push Protection bloqueia isso automaticamente, e com razão).
  AWS_ACCESS_KEY_ID: "REPLACE_WITH_YOUR_OWN"
  AWS_SECRET_ACCESS_KEY: "REPLACE_WITH_YOUR_OWN"
  AWS_REGION: "us-east-1"
